const cible = scene.children[0].getObjectByName("mesh_526");
// //miroir.name = "miroir";
console.log(scene.children[0]);
//scene.children[0].getObjectByName('mesh_526_1').material.color.set("blue");
//scene.children[0].getObjectByName('mesh_526_1').material.emissiveIntensity =1;
// miroir.material.metalness = .3;

// Assuming you have a scene set up

// const meshes = [];

// const traverseHierarchy = (object) => {
//   if (object.isMesh) {
//     meshes.push(object);
//   }

//   object.children.forEach((child) => {
//     traverseHierarchy(child);
//   });
// }

// traverseHierarchy(scene);

// console.log(meshes);

//  console.log(scene);
// console.log(scene.type);
